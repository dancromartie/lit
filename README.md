Limited git - Git with heavy options checking.

Perhaps a little easier than git hooks for enforcing some aspects of your org's git strategy.

For example, I might want to only have one branch per developer plus a master branch,
and want constant merging into master with heavy use of tags.
